node_modules/
.env
data/app-data.json
data/uploads/
*.log
.DS_Store
Thumbs.db
data/bridge-session*.json
.env.bak*
*.bak

# bridge 的资料库镜像:它把用户在 App 里上传的文档写成真文件,落在 bridge 的工作目录下。
# 如果有人直接在仓库根目录跑 bridge(很常见的试用方式),那就是**用户的私密文档
# 躺在 git 工作区里** —— 一个 `git add -A` 就公开了。这里挡死。
资料库/
.asset-backups-badges/
